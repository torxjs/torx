File content:

@{
    const data = file('./test/5/file.json');
    const json = JSON.parse(data);
    @// print(Object.keys(__data));
    @// print(typeof json);
    @// print(Object.keys(fs));
}

Hello?
@// @file('./test/5/file.txt')
<ul>
    @for (const key in json.list) {
        <li>@key</li>
    }
</ul>
