#!/usr/bin/env node
/**
 * Generate the out path based on two arguments
 * @param arg1 - file path ending in ".torx"
 * @param arg2 - out file path or file extension
 */
export declare function getOutPath(arg1: string, arg2?: string): string;
