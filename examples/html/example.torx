@{
    const title = "Example";
}
@function button(label: string) {
    <button>@label</button>
}
<!DOCTYPE html>
<html>
    <head>
    </head>
    <body>
        <h1>@title</h1>
        @for (const key in ['a','b','c']) {
            @button(key)
        }
    </body>
</html>
